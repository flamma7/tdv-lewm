defaults:
  - _self_
  - launcher: local
  - data: ogb

output_model_name: lewm
subdir: ${hydra:job.id}

num_workers: 6
train_split: 0.9
seed: 3072
img_size: 224
patch_size: 14
encoder_scale: tiny
embed_dim: 192

# Leftover from original code
encoder_resnet9: False
train_value_function: False
projector_loss_weight: 1e-3
dump_object: False
compile: true
make_it_fast: false
log_throughput: False # logs samples/sec and other metrics
# Resume trainer state from HF {repo_id}/{path_prefix}/{output_model_name}/latest.ckpt
load_checkpoint_hf: false

trainer:
  max_epochs: 10
  devices: 1
  accelerator: gpu
  precision: bf16-mixed
  gradient_clip_val: 1.0
  check_val_every_n_epoch: 2

loader:
  batch_size: 128
  num_workers: ${num_workers}
  drop_last: True
  persistent_workers: True
  prefetch_factor: 3
  pin_memory: True
  shuffle: True

val_loader:
  batch_size: ${loader.batch_size}
  num_workers: 4
  drop_last: False
  persistent_workers: True
  prefetch_factor: 1
  pin_memory: ${loader.pin_memory}
  shuffle: False

optimizer:
  type: AdamW
  lr: 5e-5
  weight_decay: 1e-3
  fused: false # must be false for gradient clipping

wm:
  history_size: 3
  num_preds: 1

model:
  _target_: stable_worldmodel.wm.lewm.LeWM
  encoder:
    _target_: stable_pretraining.backbone.utils.vit_hf
    size: ${encoder_scale}
    patch_size: ${patch_size}
    image_size: ${img_size}
    pretrained: false
    use_mask_token: false
  predictor:
    _target_: stable_worldmodel.wm.lewm.module.Predictor
    num_frames: ${wm.history_size}
    input_dim: ${embed_dim}
    hidden_dim: ${embed_dim}
    output_dim: ${embed_dim}
    depth: 6
    heads: 16
    mlp_dim: 2048
    dim_head: 64
    dropout: 0.1
    emb_dropout: 0.0
  action_encoder:
    _target_: stable_worldmodel.wm.lewm.module.Embedder
    input_dim: ???
    emb_dim: ${embed_dim}
  projector:
    _target_: stable_worldmodel.wm.lewm.module.MLP
    input_dim: ${embed_dim}
    output_dim: ${embed_dim}
    hidden_dim: 2048
    norm_fn:
      _target_: torch.nn.BatchNorm1d
      _partial_: true
  pred_proj:
    _target_: stable_worldmodel.wm.lewm.module.MLP
    input_dim: ${embed_dim}
    output_dim: ${embed_dim}
    hidden_dim: 2048
    norm_fn:
      _target_: torch.nn.BatchNorm1d
      _partial_: true

loss:
  sigreg:
    weight: 0.09
    kwargs:
      knots: 17
      num_proj: 1024
    # Collapse scalars on post-projection embeddings: variance_mean,
    # effective_rank_frac, mean_rms. CPU buffers, not in the training loss.
    diagnostics:
      enabled: true
      buffer_size: 8192
      sample_every: 16
      sample_size: 48
      log_every: 400
      min_samples: 2048
      eps: 1.0e-12
      background: true
